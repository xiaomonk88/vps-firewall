# 更新记录

## 1.0.8（2026.09.16）

- 修复改名升级时旧版 `vps-whitelist` 服务未停用、与新版反复覆盖同一规则表的问题：安装时识别并停止旧服务、取消其开机启动，保留旧配置和文件。
- 主菜单在操作锁内读取配置、成功记录及实际规则，状态命令额外显示保存的开关、实际规则和旧服务冲突。
- 设置与实际规则不一致时显示黄色“未生效 / 未关闭”，菜单提供“恢复防护 / 关闭残留规则”，不再误标为正常关闭。
- 后台规则丢失时恢复已验证快照，保留回滚记录并记录恢复原因；没有启用的省份时不做周期性规则重建。

## 1.0.7（2026.09.16）

- IP、网段和省份白名单新增 `5 暂停条目`、`6 启用条目`，支持按编号批量操作。
- 暂停时保留条目和备注，不参与放行规则；启用后恢复使用，状态随配置备份、重启和回滚保留。
- 名单表格新增“状态”列，权限检查忽略已暂停来源，暂停省份不再下载数据。
- 兼容旧配置，原有条目默认启用；暂停涉及当前 SSH 权限时保留确认提示。

## 1.0.6（2026.09.16）

- IP、网段和省份白名单支持逐条备注，添加时可填写，也可留空。
- 列表预览、完整列表、移除确认和运行状态新增备注列，长备注自动折行。
- 名单菜单新增 `4 修改备注`，支持补充、修改和清空；删除条目时同步清理备注。
- 兼容没有备注的旧配置；备注随配置保存、备份及回滚。

## 1.0.5（2026.09.16）

- 底部“防火墙”靠左，“当前IP”靠右，与上方表格右边框对齐。
- 对齐按中文显示宽度计算，保留开启绿色、关闭红色；窄终端空间不足时自动换行。

## 1.0.4（2026.09.16）

- 底部状态合并为一行：`防火墙：开启/关闭  当前IP：地址`。
- 支持颜色的交互终端中，“开启”显示绿色，“关闭”显示红色，仅状态文字着色。
- 状态根据本程序实际规则表判断；配置同步提醒单独显示。重定向输出时保留纯文本。

## 1.0.3（2026.09.16）

- 主菜单按 IP管理、系统控制、版本控制三个板块分组，使用表格横线分隔，保留原操作编号。
- 防护状态和当前登录 IP 移至菜单底部。
- 顶部显示程序版本和本版修改日期，子菜单名称单独显示。

## 1.0.2

- 主菜单新增 `8 更新程序`，确认后更新到最新正式版，完成后自动重新打开菜单。
- 主菜单新增 `9 卸载程序`，确认后执行卸载，保留配置、缓存和历史备份。
- 更新、卸载取消后返回主菜单；卸载完成或操作失败后退出菜单并显示结果。

## 1.0.1

- 主菜单、子菜单标题和运行状态显示当前程序版本。
- 新增 `vps-firewall --version`，保留 `vps-firewall version`。
- 各处统一读取 `VERSION`，便于更新后确认安装版本。

## 1.0.0

- 首次发布 VPS 入站防火墙和中文管理菜单。
- 支持从 GitHub 一键安装，以及 `vps-firewall update` 更新。
- 提供自动测试和 GitHub Release 发布流程。
