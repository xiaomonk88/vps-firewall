# vps-firewall：当前实现与修复说明

更新日期：2026-09-16。本说明已更新为改造后的源码行为，替代此前的旧版问题梳理。

## 1. 使用目标

将目录上传到 Debian 12/13，执行 `sudo bash install.sh`，然后输入 `sudo vps-firewall` 进入中文管理菜单。依赖、配置创建、规则校验、服务安装和开机恢复由安装脚本完成。

不带参数现在默认打开菜单。原有 `menu`、`config`、`apply`、`reload`、`status`、`test`、`check`、`rollback` 命令仍可使用。

## 2. 文件目录

| 文件 | 作用 |
| --- | --- |
| `vps_firewall.py` | 配置校验、省份数据、规则生成、事务恢复、命令行和中文菜单 |
| `install.sh` | 安装/升级，保留已有配置，迁移旧版系统规则文件 |
| `uninstall.sh` | 停止服务、清除本程序规则、备份用户数据并卸载 |
| `vps-firewall.service` | systemd 服务，启动前恢复成功规则，后台监测配置 |
| `config.example.toml` | 配置格式参考；安装程序自行生成实际配置 |
| `README.md` | 面向使用者的安装和操作说明 |
| `tests/test_vps_firewall.py` | 不触碰主机防火墙的跨平台回归测试 |
| `tests/test_linux_network.py` | Linux 隔离网络中的真实 TCP / nftables 测试 |

## 3. 白名单与禁止列表

管理/抢救地址、省份网段、手动网段、单 IP 取并集；来源匹配任意一个即可。IPv4 与 IPv6 分别生成集合，重叠网段先归并，避免相互冲突。

新增 `blocked_ips`，仅接受单个 IPv4/IPv6，优先于普通白名单、省份以及“全部 IPv6 放行”。禁止地址不得覆盖管理地址。管理地址可以调整和删除，但开启状态至少需要保留一个。

首次安装只在生成配置时把识别到的 SSH 来源保存为可见的管理地址。运行时不再扫描 22 端口、不再动态追加已删除地址。SSH 来源识别支持环境变量和当前进程祖先链中的 SSH 信息，不依赖 SSH 服务端口。

### 删除固定 IP 的修复

旧版有三个可能造成“删除后仍可访问”的因素：

1. 无条件放行所有已建立连接。
2. 每次重载又将当前 SSH 来源加入名单。
3. 相同地址仍被管理地址、网段、省份或全部 IPv6 开关覆盖。

现在只对服务器主动连接的回复方向保留已建立连接例外；外部主动连接的后续入站包也要匹配最新名单。删除后不再自动加回 SSH 来源。菜单会解释重叠放行，提供“彻底撤销权限”选项，或允许用户明确保留其他覆盖。

“彻底撤销”会删除重复的单 IP 管理地址并加入禁止列表；若会删除最后一个管理地址，操作会拒绝并要求先添加备用管理地址。若冲突的是管理网段，需要先调整该网段。影响当前 SSH 来源时还会询问断线确认。

这里的撤销是阻断后续数据包，未主动发送 TCP RST，也不会撤回已经到达应用的数据。ping 的公网例外与业务连接独立。

## 4. 中文交互菜单

主菜单为纵向单列表格，每行一个功能，按“编号、功能、数量/状态”分栏。IP管理（1–4）、系统控制（5–7）、版本控制（8–9）三个板块以跨列表头和横线分隔，`0` 单独用于退出。标题、子菜单、列表和确认操作统一使用边框符号，中文按实际显示宽度对齐，长地址自动折行。顶部显示版本号及本版修改日期；防护状态和当前登录 IP 位于菜单底部，必要的配置同步提醒显示在状态之前。

| 编号 | 操作 |
| --- | --- |
| 1 | 白名单管理：进入 IP白名单、网段白名单、省份白名单子菜单 |
| 2 | 禁止 IP：明确覆盖普通白名单的放行 |
| 3 | 直通IP地址：原管理登录地址，维护管理来源和备用地址 |
| 4 | IP 权限检查：根据上次成功规则判断业务访问，并显示覆盖线索 |
| 5 | 访问设置：公网 ping、全部 IPv6 放行、Docker/NAT 映射端口保护 |
| 6 | 维护与日志：运行状态、刷新省份、恢复上一版、最近日志 |
| 7 | 暂停/开启防护，状态持久保存 |
| 8 | 更新程序：确认后安装最新正式版，完成后重新打开菜单 |
| 9 | 卸载程序：确认后备份数据并卸载，完成后退出菜单 |
| 0 | 退出或返回 |

“白名单管理”子菜单依次为 `1 IP白名单`、`2 网段白名单`、`3 省份白名单`，分别显示条目数，`0` 返回主菜单。三类名单编辑完返回白名单管理，其他列表编辑完返回主菜单。

名单页面先预览最多 5 项，再提供纵向的“添加”“移除”“查看完整列表”“返回”。添加和移除有各自的页面；只有查看完整列表或选择移除时才显示条目编号，避免与操作编号混淆。

完整列表、移除选择、省份选择每页 12 项，`n` 下一页、`p` 上一页，编号跨页保持一致。移除支持多个编号或范围，例如 `1 3`、`2-5`。省份页标记已添加省份，也可直接输入名称。

`0` 统一用于返回或取消，确认操作使用 `1` 确认、`0` 取消，回车默认取消。主菜单和操作菜单回车仅刷新当前页，不退出。操作结果保留在当前菜单，不要求多按一次回车；状态和日志等只读结果页按回车返回。交互终端切页清理当前屏幕，保留终端滚动历史；重定向输出时不使用清屏控制字符。

一次批量输入中只要存在非法项目，就不保存该批次。修改立即校验、保存并应用，无需返回主菜单才生效。防护暂停时明确提示“已保存，开启后生效”。菜单写配置时会重新整理文本格式和注释；不支持未知配置项。

## 5. 配置项

| 项目 | 默认值 | 含义 |
| --- | --- | --- |
| `enabled` | `true` | 是否启用过滤 |
| `rescue_ips` | 安装时填写/识别 | IPv4/IPv6 管理地址或网段；开启时至少保留一个 |
| `provinces` | `[]` | IPv4 省份白名单 |
| `cidrs` | `[]` | IPv4/IPv6 网段 |
| `ips` | `[]` | IPv4/IPv6 单 IP |
| `blocked_ips` | `[]` | 明确禁止的单 IP |
| `allow_ping` | `true` | 对公网放行 echo-request，关闭后白名单地址仍可 ping |
| `allow_all_ipv6` | `false` | 整体放行 IPv6 业务入站，禁止列表仍优先 |
| `protect_dnat` | `true` | 对经过 DNAT 的入站映射端口使用相同名单 |
| `watch_interval` | `5` | 配置轮询间隔，最小 5 秒 |
| `province_refresh` | `86400` | 省份刷新周期；0 禁用自动刷新，保留手动刷新 |
| `province_source_base` | 空字符串 | 优先尝试的自定义 HTTP(S) 数据源 |

配置必须符合类型和取值规则；非法地址、未知省份、管理地址与禁止列表冲突会拒绝整次应用，避免静默跳过后产生意外权限。

## 6. 防火墙规则范围

本程序仅创建/替换 `inet vps_wl` 表。通过单个 nft 事务删除并重建自己的表，避免重复叠加规则；不会执行全局 `flush ruleset`。

本机 `input` 链依次放行回环、丢弃无效连接及禁止来源、放行出站连接的回复、保留必要的 IPv6 邻居发现/相关 ICMP 错误、处理公网 ping 和 IPv6 总开关，再匹配 IPv4/IPv6 白名单，最终丢弃其他流量。

默认还创建 `forward` 链，仅将 `ct status dnat` 且属于连接原始方向的流量送往同一白名单检查。这覆盖常见 Docker/NAT 映射端口，包括既有连接；不控制普通转发、无 DNAT 的容器路由直连或云安全组。服务器和容器主动连接的普通回包不会因移除宽泛的 established 放行而被一律阻断。

禁止来源规则也会拦截 VPS 主动连接该来源时的回复，这是明确禁止模式的当前行为。其他防火墙可以进一步拒绝本程序已放行的访问。

## 7. 应用、备份与恢复

- 菜单、守护进程、手动重载和回滚使用同一个文件锁，避免同时写入规则与配置。
- 先构建并校验规则，成功后再进行应用与保存；检测下载期间的配置变动，避免覆盖其他编辑。
- 配置和状态文件通过临时文件、刷新落盘、原子替换写入。
- `pending.json` 记录尚未完成的事务；应用或保存失败时恢复原规则，菜单操作同时恢复原配置。进程中断后下一次应用/启动尝试恢复。
- `state.json` 保留当前和上一版成功的配置、规则及应用时间。未改变策略的重复重载不会覆盖上一版备份。
- 回滚同时恢复配置和当时的规则，包括当时省份集合；不会恢复/覆盖其他工具的整份防火墙。
- 开机首先恢复最近一次成功规则，不依赖实时下载；随后由守护进程处理配置变化和周期刷新。
- 菜单遇到损坏配置时可选择恢复最近一次成功配置。

手动编辑 TOML 后若校验失败，文件保留用户编辑内容，现有规则保持不变，日志提示并重试。没有实现“自动检测 SSH 是否还能登录后再撤销修改”的远程连通性确认；用户仍需保留真实可用的备用管理来源。

## 8. 省份数据

仍使用 `metowolf/iplist`，自定义源优先，其次尝试内置源和镜像。下载内容必须全部为有效 IPv4 地址/网段，拒绝空文件、HTML 错误页和无效数据。

默认 24 小时刷新。下载失败可使用旧缓存，且不更新旧缓存时间；没有可用缓存时整次应用失败，保留旧规则。每个下载请求超时 12 秒；多个省份和多个源失败会延长等待时间。省份映射准确度和第三方源可用性无法由本程序保证。

## 9. 安装、升级和卸载

新安装会自动安装 `nftables`、`python3`、`ca-certificates`，写入主程序和服务文件。执行 nft 语法校验及实际应用成功后才报告完成。首次输入管理地址支持 IPv4/IPv6，多个以空格或逗号分隔。

重复安装保留已有配置；缺少管理地址的旧版启用配置需要补充管理地址。旧版未知/非法字段不会被静默忽略，需要按错误提示修正。

新版不再将自己的规则写入 `/etc/nftables.conf`。升级时，仅当该文件被识别为旧版本生成的完整单表内容，才恢复旧版保存的原始文件，并保存迁移副本；混合自定义规则会停止自动迁移，避免覆盖其他内容。

卸载先处理旧版系统配置，再删除本程序规则；配置、缓存和历史复制到独立的 `/var/lib/vps-firewall-uninstalled.XXXXXX/` 后才删除安装目录。卸载不会递归删除上传目录。删除规则或备份失败会终止，不报告成功。

| 安装路径 | 用途 |
| --- | --- |
| `/opt/vps-firewall/` | 程序和卸载脚本 |
| `/usr/local/bin/vps-firewall` | 菜单/命令入口 |
| `/etc/vps-firewall/config.toml` | 用户配置 |
| `/etc/systemd/system/vps-firewall.service` | 服务定义 |
| `/var/lib/vps-firewall/province_cache/` | 省份缓存 |
| `/var/lib/vps-firewall/state.json` | 当前及上一版成功配置、规则 |
| `/var/lib/vps-firewall/pending.json` | 仅未完成事务期间存在的恢复记录 |
| `/run/lock/vps-firewall.lock` | 并发操作锁 |

## 10. 原问题修复对应表

| 原编号 | 处理结果 |
| --- | --- |
| R01 抢救 IP 保护描述不实 | 明确管理地址的可编辑行为，开启至少保留一个；与禁止列表冲突拒绝 |
| R02 SSH 仅识别 IPv4 22 端口 | 改为安装时识别来源和菜单提示；兼容自定义端口/IPv6，不再运行时自动放回 |
| R03 卸载先删除备份 | 先迁移恢复，数据另行备份后再删除 |
| R04 重装覆盖配置 | 已有配置保留，缺失字段使用兼容默认值 |
| R05 覆盖公共规则文件 | 使用独立表和服务启动恢复，正常运行不写系统 nftables.conf |
| R06 回滚不精确 | 同时还原本程序完整规则及配置，不叠加旧规则 |
| R07 IPv6 网段进入 IPv4 集合 | IPv4/IPv6 分开校验和生成集合 |
| R08 IPv6 行为与说明不一致 | 支持 IPv6 白名单，保留必要控制报文，明确 ping 和总放行例外 |
| R09 失败后不重试 | 失败不标记为已同步，保留旧规则，后台重试 |
| R10 初次配置错误导致异常 | 轮询间隔预先初始化，配置错误记录日志后继续运行 |
| R11 服务运行不等于规则生效 | 安装同步校验、应用及启动恢复；状态显示配置同步情况，关闭检查 nft 返回码 |
| R12 重叠网段 | 按 IP 版本归并重叠/相邻网段后生成 interval 集合 |

## 11. 测试与当前验证边界

跨平台测试覆盖配置类型、地址删除、禁止优先级、IPv6、归并网段、省份缓存、菜单批量输入、并发配置冲突、失败恢复、回滚、启动恢复和旧版迁移。执行：

```bash
python3 -m unittest discover -s tests -p test_vps_firewall.py -v
```

Linux 网络测试使用三个临时网络命名空间，运行真实 TCP 服务和 nftables，检查 IPv4/IPv6 既有连接撤权、出站响应、DNAT 映射访问及无关防火墙表保留：

```bash
# 需要 root、nftables、iproute2，以及内核网络命名空间权限
sudo python3 -m unittest discover -s tests -p test_linux_network.py -v
```

测试仅在临时命名空间中创建规则和虚拟接口，完成后清理；不会清空宿主机防火墙。缺少 Linux/权限/工具时会显示跳过。

本次开发环境为 Windows，已运行跨平台测试和 Bash/Python 语法检查；Linux 网络测试未实际执行。未连接真实 VPS，未宣称已验证 Debian 重启、Docker 实例或真实公网连通性。安装中的 `nft -c` 是目标环境语法检查，实际访问效果仍需用两个不同来源 IP 验证。
